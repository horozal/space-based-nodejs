const InMemoryGrid = require('./InMemoryGrid');

// Create an instance of the in-memory data grid
const dataGrid = new InMemoryGrid();

// Example usage of the in-memory data grid
dataGrid.put('key1', 'value1');
dataGrid.put('key2', 'value2');
